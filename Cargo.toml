[package]
name = "pulse"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "cookies"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
local-ip-address = "0.6"
